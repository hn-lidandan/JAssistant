# 领域驱动设计 (DDD) 编程约束

本规范旨在为基于领域驱动设计 (DDD) 的项目提供编程约束和最佳实践。

### 1. 限界上下文 (Bounded Context)

*   **【强制】** 每个限界上下文应有明确的边界，并独立建模。
*   **【推荐】** 限界上下文之间通过明确定义的接口进行交互，避免直接的数据共享。

### 2. 实体 (Entity)

*   **【强制】** 实体应具有唯一的标识符 (ID)，其生命周期是连续的。
*   **【强制】** 实体应封装行为和数据，而不是仅仅作为数据容器。
*   **【推荐】** 避免贫血模型 (Anemic Domain Model)，即只有 Getter/Setter 的实体。

### 3. 值对象 (Value Object)

*   **【强制】** 值对象应是不可变的 (Immutable)。
*   **【强制】** 值对象通过其属性值来判断相等性，而不是通过引用相等性。
*   **【推荐】** 优先使用值对象来表示概念，而不是实体，以减少复杂性。

### 4. 聚合 (Aggregate)

*   **【强制】** 聚合应有一个聚合根 (Aggregate Root)，它是聚合中唯一可以通过 ID 直接访问的实体。
*   **【强制】** 聚合内部的实体和值对象应通过聚合根进行操作，外部不应直接访问聚合内部的非根实体。
*   **【强制】** 聚合应保持内部数据的一致性，所有业务规则的校验应在聚合根中完成。
*   **【推荐】** 聚合应尽可能小，以减少并发冲突和提高性能。

### 5. 领域服务 (Domain Service)

*   **【强制】** 领域服务应封装跨多个实体或值对象的领域逻辑，这些逻辑不适合放在任何一个实体或值对象中。
*   **【推荐】** 领域服务应是无状态的。
*   **【推荐】** 领域服务的命名应体现其行为，例如 `TransferService`, `OrderPlacementService`。

### 6. 仓储 (Repository)

*   **【强制】** 仓储应提供聚合的持久化和查询机制。
*   **【强制】** 仓储的接口应定义在领域层，实现应在基础设施层。
*   **【推荐】** 仓储应以聚合为单位进行操作，而不是单个实体。

### 7. 领域事件 (Domain Event)

*   **【推荐】** 领域事件用于表示领域中发生的有意义的事情，可以用于解耦不同的限界上下文或聚合。
*   **【推荐】** 领域事件应是不可变的，并且包含事件发生时的所有相关数据。

### 8. 通用语言 (Ubiquitous Language)

*   **【强制】** 在团队、代码和领域模型中，应始终使用统一的通用语言。
*   **【推荐】** 领域专家、开发人员和业务人员之间应共同维护和演进通用语言。

