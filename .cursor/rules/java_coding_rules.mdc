
# Java 编码与 RESTful API 设计规范

本规范结合了 Google Java 编码规范、阿里巴巴 Java 开发手册以及业界广泛认可的 RESTful API 设计准则，旨在提高代码质量、可读性、一致性和协作效率。

---

## 第一部分：Java 编码规范

本部分主要参考 Google 和阿里巴巴的编码规范。

### 1. 命名规范 (Naming Conventions)

*   **包名 (Package):** 全部小写，连续的单词直接连接，不使用下划线。例如: `com.company.project.module`。
*   **类名和接口名 (Class & Interface):** 采用大驼峰命名法 (UpperCamelCase)。例如: `MyClass`, `UserService`。
*   **方法名 (Method):** 采用小驼峰命名法 (lowerCamelCase)。例如: `getUserProfile`, `calculateTotalAmount`。
*   **常量名 (Constant):** 全部大写，单词间用下划线分隔。例如: `MAX_RETRY_COUNT`。
*   **变量名 (Variable):** 采用小驼峰命名法。例如: `userName`, `requestData`。
*   **布尔变量:** 推荐以 `is`, `has`, `can` 等作为前缀。例如: `isEnabled`, `hasPermission`。

### 2. 代码格式 (Code Formatting)

*   **缩进:** 使用 **2个空格** 进行缩进，不使用 Tab。
*   **列宽:** 每行代码不超过 **100个字符**。
*   **大括号:**
    *   左大括号 (`{`) 不换行，紧跟在类、方法或条件语句的末尾。
    *   右大括号 (`}`) 单独占一行。
    *   即使只有一行代码，`if`, `else`, `for`, `while` 等语句也必须使用大括号。
*   **导包 (Imports):**
    *   禁止使用通配符 `*` 导包。
    *   `import` 语句按以下顺序分组，组间用空行分隔：
        1.  `java` 和 `javax` 包
        2.  第三方库 (`org`, `com`, `io`, etc.)
        3.  项目内部包 (`tld.sld.asset`)

### 3. 面向对象编程 (OOP)

*   一个源文件只包含一个顶级类或接口。
*   遵循 SOLID 原则。
*   优先使用接口编程，而不是具体类。
*   避免在构造函数中进行复杂的业务逻辑。

### 4. 异常处理与日志 (Exception Handling & Logging)

*   **异常:**
    *   不要捕获 `Exception` 或 `Throwable`，应捕获具体的异常类。
    *   不要“生吞”异常 (即空的 `catch` 块)。如果确实不需要处理，应在日志中记录。
    *   自定义异常应继承自 `RuntimeException` 或合适的业务异常基类。
*   **日志:**
    *   使用 SLF4J 作为日志门面，配合 Logback 或 Log4j2 实现。
    *   日志级别需谨慎选择：`DEBUG` 用于开发调试，`INFO` 用于记录关键业务流程，`WARN` 用于潜在问题，`ERROR` 用于严重错误。
    *   禁止在日志中直接输出敏感信息 (如密码、身份证号等)。

### 5. 其他

*   禁止使用魔法值 (Magic Number)，应定义为常量。
*   使用 `Objects.equals()` 来比较对象，避免 `NullPointerException`。
*   集合初始化时，尽量指定初始容量以提高性能。

---

## 第二部分：RESTful API 设计规范

### 1. URI (Uniform Resource Identifier)

*   **使用名词而非动词:** URI 代表资源，应使用名词。HTTP 方法 (GET, POST, PUT, DELETE) 定义对资源的操作。
    *   **推荐:** `GET /users/123`
    *   **不推荐:** `GET /getUserById?id=123`
*   **使用复数名词:** 集合类资源使用复数名词。
    *   **推荐:** `/users`, `/orders`
    *   **不推荐:** `/user`, `/order`
*   **路径格式:**
    *   全部使用小写字母。
    *   单词之间使用连字符 (`-`) 分隔 (kebab-case)。
    *   **推荐:** `/user-profiles`
    *   **不推荐:** `/user_profiles`, `/userProfiles`
*   **层级关系:** 通过 `/` 表示资源的层级关系。
    *   例如: `GET /users/123/orders` (获取用户123的所有订单)
    *   建议层级不易过深，一般不超过2-3级。

### 2. HTTP 方法 (HTTP Methods)

*   **GET:** 读取/查询资源。具有安全性和幂等性。
*   **POST:** 创建资源。非幂等。
*   **PUT:** 全量更新/替换资源。具有幂等性。
*   **PATCH:** 部分更新资源。具有幂等性。
*   **DELETE:** 删除资源。具有幂等性。

### 3. HTTP 状态码 (HTTP Status Codes)

*   **2xx (成功):**
    *   `200 OK`: 请求成功。
    *   `201 Created`: 资源创建成功。
    *   `204 No Content`: 请求成功，但响应体中无内容 (如 DELETE 成功后)。
*   **4xx (客户端错误):**
    *   `400 Bad Request`: 请求参数错误或语法无效。
    *   `401 Unauthorized`: 未认证或认证失败。
    *   `403 Forbidden`: 已认证，但无权限访问。
    *   `404 Not Found`: 请求的资源不存在。
*   **5xx (服务端错误):**
    *   `500 Internal Server Error`: 服务器内部发生未知错误。

### 4. API 版本控制 (Versioning)

*   将版本号放入 URI 中是一种清晰且常用的方式。
    *   例如: `https://api.example.com/v1/users`

### 5. 查询参数 (Query Parameters)

*   使用查询参数实现对集合资源的过滤、排序和分页。
    *   **过滤:** `GET /users?status=active`
    *   **排序:** `GET /users?sort=-createdAt` (按创建时间降序)
    *   **分页:** `GET /users?page=1&limit=20`

### 6. 响应体格式 (Response Body)

*   推荐使用统一的 JSON 响应结构，方便客户端处理。
    ```json
    {
      "success": true,
      "code": 200,
      "message": "操作成功",
      "data": {
        "id": 123,
        "name": "John Doe"
      }
    }
    ```
    或在失败时：
    ```json
    {
      "success": false,
      "code": 40001, // 业务错误码
      "message": "用户名不能为空",
      "data": null
    }
    ```
