# 命名规范 (Naming Conventions)

本规范基于阿里巴巴Java开发手册。

### 1. 通用命名规则

*   **【强制】** 代码中的命名均不能以`_`或`$`开始或结束。
*   **【强制】** 代码中的命名严禁使用拼音与英文混合的方式，更不允许直接使用中文。
*   **【强制】** 类名、方法名、参数名、成员变量、局部变量都统一使用驼峰式（CamelCase）风格。
*   **【强制】** 常量命名应该全部大写，单词间用`_`隔开，力求语义表达完整清楚，不要嫌名字长。
*   **【推荐】** 如果使用到了设计模式，建议在类名中体现出具体模式。

### 2. 包名 (Package)

*   **【强制】** 包名统一使用小写，点分隔符之间有且仅有一个自然语义的英语单词。包名统一使用单数形式，但是类名如果有复数含义，类名可以使用复数形式。

### 3. 类名与接口名 (Class & Interface)

*   **【强制】** 类名使用大驼峰（UpperCamelCase）风格，但以下情形例外：DO / BO / DTO / VO / AO / PO / UID 等。
*   **【强制】** 接口和实现类的命名规则：
    1.  对于 Service 和 DAO 类，基于 SOA 的理念，暴露出来的服务一定是接口，内部的实现类用 Impl 的后缀与接口区别。
    2.  如果是形容能力的接口，取对应的形容词做接口名（通常是 -able 的形式）。

### 4. 方法名 (Method)

*   **【强制】** 方法名、参数名、成员变量、局部变量都统一使用小驼峰（lowerCamelCase）风格。
*   **【推荐】** 方法名应动宾结合，明确表达方法的作用。

### 5. 常量 (Constant)

*   **【强制】** 常量命名应该全部大写，单词间用`_`隔开。

### 6. 变量 (Variable)

*   **【强制】** 变量名使用小驼峰（lowerCamelCase）风格。
*   **【推荐】** POJO 类中的任何布尔类型的变量，都不要加 `is` 前缀，否则部分框架解析会引起序列化错误。

