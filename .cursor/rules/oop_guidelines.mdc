# 面向对象编程 (OOP) 规范

本规范基于阿里巴巴Java开发手册。

### 1. 静态变量与静态方法

*   **【强制】** 静态变量或方法应直接用类名来调用，而不是通过对象名来调用。

### 2. 构造函数

*   **【推荐】** 不要在构造函数中进行复杂的业务逻辑初始化。

### 3. `equals` 方法

*   **【强制】** `equals` 方法必须满足自反性、对称性、传递性、一致性以及对于任何非 `null` 的 `x`，`x.equals(null)` 必须返回 `false`。
*   **【强制】** 所有的 POJO 类属性必须使用包装数据类型。
*   **【强制】** RPC 方法的返回值和参数必须使用包装数据类型。
*   **【推荐】** 推荐使用 `Objects.equals()` 来防止 `NullPointerException`。

### 4. `hashCode` 方法

*   **【强制】** `hashCode` 和 `equals` 的相关规定：
    1.  如果重写了 `equals`，就必须重写 `hashCode`。
    2.  因为 Set 存储的是不重复的对象，依据 `hashCode` 和 `equals` 进行判断，所以 Set 存储的对象必须重写这两个方法。

### 5. `toString` 方法

*   **【推荐】** POJO 类建议重写 `toString` 方法，如果继承了另一个 POJO 类，注意在 `super.toString()` 的基础上扩展。

### 6. 序列化

*   **【强制】** POJO 类必须实现 `Serializable` 接口。
*   **【强制】** `serialVersionUID` 不允许修改，除非是完全不兼容的两个版本。

### 7. 接口与实现

*   **【推荐】** 优先面向接口编程，而不是面向实现类编程。

### 8. SOLID 原则

*   **【推荐】** 遵循单一职责、开闭、里氏替换、接口隔离、依赖倒置原则。

